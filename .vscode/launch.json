{
    "version": "0.2.0",
    "configurations": [
        {
            "type": "node-terminal",
            "name": "Backend",
            "request": "launch",
            "command": "npm run start",
            "cwd": "${workspaceFolder}\\backend"
        }
    ]
}